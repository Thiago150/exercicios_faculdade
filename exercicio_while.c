#include <stdio.h>

int main(){
    int cont = 0;

    // Será executado quando cont for menor que zero.
    while (cont < 10){
        printf("\n Programa");

        // inclementa cont para que não entre em loop infinito.
        cont ++;
        /* code */
    }
    return 0;


}